NODE_ENV=development
PORT=4000
HOST=0.0.0.0

# Provider selection
DEFAULT_PROVIDER=consumet

# Redis (optional — falls back to in-memory cache when disabled)
REDIS_ENABLED=false
REDIS_URL=redis://localhost:6379

# Cache TTL in seconds
CACHE_TTL_SECONDS=300
